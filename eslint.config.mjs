import unjs from 'eslint-config-unjs'

export default unjs({
  ignores: ['dist', 'node_modules', 'temp'],
  rules: {
    // rule overrides
  },
  markdown: {
    rules: {
      // markdown rule overrides
    },
  },
})
